```
continued fraction cli implemented in rust

each command looks like this:

`<decimal number> [mode]`

modes:
 - f|frac|fraction: show just the answer (default);
 - c|conv|convergents|cf: show the answer and the convergents used in the algorithm.

 ```
